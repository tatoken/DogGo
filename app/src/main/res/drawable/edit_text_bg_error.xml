<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" /> <!-- Colore di sfondo -->
    <stroke
        android:width="1dp"
        android:color="@color/primaryColor" /> <!-- Colore del bordo -->
    <corners android:radius="8dp" /> <!-- Angoli non arrotondati -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>