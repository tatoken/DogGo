<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" /> <!-- Colore di sfondo -->
    <stroke
        android:width="1dp"
        android:color="@color/myGray" /> <!-- Colore del bordo -->
    <corners android:radius="8dp" /> <!-- Angoli non arrotondati -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
</shape>